[package]
name = "bindings"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
sop = { version = "0.1.0", path = "../rust" }
