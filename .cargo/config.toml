[env]
# pyo3's build script must be configured against the interpreter the SDK
# targets (3.15-only APIs are cfg-gated), not whatever `python3` resolves to.
# An exported PYO3_PYTHON still takes precedence over this default.
PYO3_PYTHON = "python3.15"
